<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="!localstack">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="localstack">
        <appender name="CLOUDWATCH" class="ca.pjer.logback.AwsLogsAppender">
            <logGroupName>productos-log-group</logGroupName>
            <logStreamName>microservicio-productos</logStreamName>

            <logRegion>us-east-1</logRegion>
            <cloudWatchEndpoint>http://localstack:4566</cloudWatchEndpoint>

            <accessKeyId>test</accessKeyId>
            <secretAccessKey>test</secretAccessKey>

            <maxBatchLogEvents>50</maxBatchLogEvents>
            <maxFlushTimeMillis>5000</maxFlushTimeMillis>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="CLOUDWATCH"/>
        </root>
    </springProfile>

</configuration>
